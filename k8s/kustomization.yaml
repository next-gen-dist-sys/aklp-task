apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  project: aklp
  managed-by: kustomize

# Optional: Add namespace
# namespace: aklp

# Optional: Add name prefix
# namePrefix: aklp-

images:
  - name: ghcr.io/yourusername/service-template
    newTag: latest